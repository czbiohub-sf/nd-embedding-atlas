// Full catalog entry: exposes every ported source as a namespace plus
// flat popular names at the top level.
//
// Import via `@ndea/ochre/catalog` for the full tree, or go narrower with
// `@ndea/ochre/catalog/<source>` (e.g. `colorcet`, `colorbrewer`).

export * from "./colormap/catalog";
