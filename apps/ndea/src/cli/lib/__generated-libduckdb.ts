// Development stub. scripts/build.ts temporarily replaces this value with
// an embedded library import while compiling, then restores this source.
export const LIBDUCKDB_EMBEDDED_PATH: string | null = null;
