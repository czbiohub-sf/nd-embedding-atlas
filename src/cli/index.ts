#!/usr/bin/env bun

/**
 * ndea CLI — entry point for nd-embedding-atlas.
 *
 * Usage:
 *   ndea view <path...>        Open zarr stores in the viewer
 *   ndea view config.yaml      Load project config
 */

console.log("ndea: not yet implemented — scaffold only");
